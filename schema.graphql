type QueueBoost @entity {
  id: ID!
  validator: String!
  amount: BigInt!
  sender: String!
  blockTime: BigInt!
  blockNumber: BigInt!
}

type THJValidator @entity {
  id: ID!
  amount: BigInt!
}

type Delegation @entity {
  id: ID!
  amount: BigInt!
}